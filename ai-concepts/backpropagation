Definition: A method used to update the weights of a neural network by calculating the gradient of the loss function with respect to each weight.

Key Role: It’s used in training neural networks to minimize the loss and improve the model’s predictions.

